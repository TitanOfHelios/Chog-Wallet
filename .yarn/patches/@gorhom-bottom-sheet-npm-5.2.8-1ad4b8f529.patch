diff --git a/src/components/bottomSheetDraggableView/BottomSheetDraggableView.tsx b/src/components/bottomSheetDraggableView/BottomSheetDraggableView.tsx
index d721fee7a0b133027df45d00d6fcf07e66b797f4..1c039146a9647191f76c7db3d98e5b9ff13452e7 100644
--- a/src/components/bottomSheetDraggableView/BottomSheetDraggableView.tsx
+++ b/src/components/bottomSheetDraggableView/BottomSheetDraggableView.tsx
@@ -33,17 +33,21 @@ const BottomSheetDraggableViewComponent = ({
     const refs = [];
 
     if (nativeGestureRef) {
+      // @ts-expect-error: Type compatibility issue with GestureType
       refs.push(nativeGestureRef);
     }
 
     if (refreshControlGestureRef) {
+      // @ts-expect-error: Type compatibility issue with GestureType
       refs.push(refreshControlGestureRef);
     }
 
     if (_providedSimultaneousHandlers) {
       if (Array.isArray(_providedSimultaneousHandlers)) {
+        // @ts-expect-error: Type compatibility issue with GestureType array
         refs.push(..._providedSimultaneousHandlers);
       } else {
+        // @ts-expect-error: Type compatibility issue with GestureType
         refs.push(_providedSimultaneousHandlers);
       }
     }
diff --git a/src/components/bottomSheetHandle/BottomSheetHandleContainer.tsx b/src/components/bottomSheetHandle/BottomSheetHandleContainer.tsx
index 022384378e2bce7a0162e15904f6e1ec18c04258..89f9bba1150965e65ef130901dadc6898331b2e9 100644
--- a/src/components/bottomSheetHandle/BottomSheetHandleContainer.tsx
+++ b/src/components/bottomSheetHandle/BottomSheetHandleContainer.tsx
@@ -43,13 +43,16 @@ function BottomSheetHandleContainerComponent({
     const refs = [];
 
     if (_internalSimultaneousHandlers) {
+      // @ts-expect-error: Type compatibility issue with handler ref
       refs.push(_internalSimultaneousHandlers);
     }
 
     if (_providedSimultaneousHandlers) {
       if (Array.isArray(_providedSimultaneousHandlers)) {
+        // @ts-expect-error: Type compatibility issue with handler refs array
         refs.push(..._providedSimultaneousHandlers);
       } else {
+        // @ts-expect-error: Type compatibility issue with handler ref
         refs.push(_providedSimultaneousHandlers);
       }
     }
diff --git a/src/components/bottomSheetModal/BottomSheetModal.tsx b/src/components/bottomSheetModal/BottomSheetModal.tsx
index 5d131f1ed227fefa637dd655b5d0e41a328aa8d5..dc1840a7ea63259c486210701f43475d1df79272 100644
--- a/src/components/bottomSheetModal/BottomSheetModal.tsx
+++ b/src/components/bottomSheetModal/BottomSheetModal.tsx
@@ -237,5 +237,8 @@ function BottomSheetModalComponent<T = any>(
         minimized.current === false &&
         !animating
       ) {
+        if (mounted.current) {
+          unmount();
+        }
         return;
       }
@@ -410,3 +413,3 @@ function BottomSheetModalComponent<T = any>(
-      if (enableDismissOnClose) {
+      if (enableDismissOnClose || forcedDismissed.current) {
         unmount();
       }
@@ -427,5 +430,6 @@ function BottomSheetModalComponent<T = any>(
     forceClose: handleForceClose,
     // modal methods
+    destroy: unmount,
     dismiss: handleDismiss,
     present: handlePresent,
     // internal
@@ -474,9 +477,7 @@ const BottomSheetModal = memo(forwardRef(BottomSheetModalComponent)) as <
   }
 ) => ReturnType<typeof BottomSheetModalComponent>;
 (
-  BottomSheetModal as React.MemoExoticComponent<
-    typeof BottomSheetModalComponent
-  >
+  BottomSheetModal as unknown as React.MemoExoticComponent<any>
 ).displayName = 'BottomSheetModal';
 
 export default BottomSheetModal;
diff --git a/src/types.d.ts b/src/types.d.ts
--- a/src/types.d.ts
+++ b/src/types.d.ts
@@ -98,8 +98,12 @@ export interface BottomSheetModalMethods<T = any> extends BottomSheetMethods {
   /**
    * Mount and present the bottom sheet modal to the initial snap point.
    * @param data to be passed to the modal.
    */
   present: (data?: T) => void;
+  /**
+   * Immediately unmount the bottom sheet modal without a close animation.
+   */
+  destroy: () => void;
   /**
    * Close and unmount the bottom sheet modal.
    * @param animationConfigs snap animation configs.
diff --git a/lib/typescript/types.d.ts b/lib/typescript/types.d.ts
--- a/lib/typescript/types.d.ts
+++ b/lib/typescript/types.d.ts
@@ -98,8 +98,12 @@ export interface BottomSheetModalMethods<T = any> extends BottomSheetMethods {
   /**
    * Mount and present the bottom sheet modal to the initial snap point.
    * @param data to be passed to the modal.
    */
   present: (data?: T) => void;
+  /**
+   * Immediately unmount the bottom sheet modal without a close animation.
+   */
+  destroy: () => void;
   /**
    * Close and unmount the bottom sheet modal.
    * @param animationConfigs snap animation configs.
diff --git a/src/hooks/useScrollHandler.ts b/src/hooks/useScrollHandler.ts
index a7910834920a83cfc49cb52b338a656c2aef4e07..18481c13a8d1550af173c76a22f880cea26f7825 100644
--- a/src/hooks/useScrollHandler.ts
+++ b/src/hooks/useScrollHandler.ts
@@ -27,6 +27,7 @@ export const useScrollHandler = (
     handleOnEndDrag = noop,
     handleOnMomentumEnd = noop,
     handleOnMomentumBegin = noop,
+    // @ts-expect-error: Type compatibility issue with AnimatedRef vs RefObject
   } = useScrollEventsHandlers(scrollableRef, scrollableContentOffsetY);
 
   // callbacks
diff --git a/src/hooks/useStableCallback.ts b/src/hooks/useStableCallback.ts
index 9202b4bf48bef2d1a03b9b279f9fb6a228560421..69177eee37fc8e0aaaf41ce11ee155ba94dfd902 100644
--- a/src/hooks/useStableCallback.ts
+++ b/src/hooks/useStableCallback.ts
@@ -8,6 +8,7 @@ type Callback<T extends unknown[], R> = (...args: T) => R;
 export function useStableCallback<T extends unknown[], R>(
   callback: Callback<T, R>
 ) {
+  // @ts-expect-error: Type compatibility issue with useRef undefined initial value
   const callbackRef = useRef<Callback<T, R>>();
 
   useLayoutEffect(() => {
@@ -16,6 +17,7 @@ export function useStableCallback<T extends unknown[], R>(
 
   useEffect(() => {
     return () => {
+      // @ts-expect-error: Type compatibility issue with undefined assignment
       callbackRef.current = undefined;
     };
   }, []);
diff --git a/src/components/bottomSheet/BottomSheet.tsx b/src/components/bottomSheet/BottomSheet.tsx
index 6748c93cac1a29c745480fe2282b8d8f6b36226f..3dbb2fd7e5268d4e6b57ea44e6c314791ff10642 100644
--- a/src/components/bottomSheet/BottomSheet.tsx
+++ b/src/components/bottomSheet/BottomSheet.tsx
@@ -659,7 +659,7 @@ const BottomSheetComponent = forwardRef<BottomSheet, BottomSheetProps>(
           status === KEYBOARD_STATUS.SHOWN &&
           position !== closedDetentPosition
         ) {
-          index = highestDetentPosition ?? DEFAULT_KEYBOARD_INDEX;
+          index = detents?.indexOf(highestDetentPosition ?? 0) ?? DEFAULT_KEYBOARD_INDEX;
         }

         /**
