interface:
  display_name: "Tmp Feature Test Release"
  short_description: "Promote fixes to Thursday tmp and run Feature Test"
  default_prompt: "Use $tmp-feature-test-release to cherry-pick a committed fix into this Thursday tmp branch and dispatch Feature Test."

policy:
  allow_implicit_invocation: true
