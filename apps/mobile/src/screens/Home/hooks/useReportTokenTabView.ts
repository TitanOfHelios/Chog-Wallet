import { HomeTabName } from '@/hooks/navigation';

export function useReportTokenTabView(params: {
  focusedTab?: HomeTabName;
  tokenDisplayModeLabel: string;
}) {
  // Telemetry reporting for the home screen has been disabled.
}
